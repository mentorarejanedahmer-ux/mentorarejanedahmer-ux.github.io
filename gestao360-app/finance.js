function dayTx(d=today()){return db.tx.filter(x=>x.date===d)}function metrics(d=today()){let day=db.cashDays[d]||{},tx=dayTx(d),open=day.open||{dinheiro:0,pix:0,cartao:0},bal={};db.accounts.forEach(a=>{let ins=tx.filter(x=>x.account===a.id&&["venda","entrada"].includes(x.type)).reduce((s,x)=>s+x.amount,0),out=tx.filter(x=>x.account===a.id&&["despesa","vale","prolabore"].includes(x.type)).reduce((s,x)=>s+x.amount,0);bal[a.id]=Number(open[a.id]||0)+ins-out});let sum=t=>tx.filter(x=>x.type===t).reduce((s,x)=>s+x.amount,0);return{sales:sum("venda"),expense:sum("despesa"),vale:sum("vale"),pro:sum("prolabore"),bal}}
function monthTx(){let p=today().slice(0,7);return db.tx.filter(x=>x.date.startsWith(p))}function month(){let t=monthTx(),sum=k=>t.filter(x=>x.type===k).reduce((s,x)=>s+x.amount,0);return{sales:sum("venda"),expense:sum("despesa"),vale:sum("vale"),pro:sum("prolabore")}}
function renderDashboard(){let m=metrics(),mm=month(),rt=db.settings.revenueTarget||0,sl=db.settings.spendingLimit||0;content.innerHTML=`<div class="grid">${db.accounts.map(a=>`<div class="card s4"><div class="muted">${a.name}</div><div class="metric">${money(m.bal[a.id])}</div></div>`).join("")}<div class="card s6"><div class="title">Faturamento do mês</div><div class="metric ok">${money(mm.sales)}</div><div class="muted">Meta ${money(rt)}</div><div class="progress"><i style="width:${rt?Math.min(100,mm.sales/rt*100):0}%"></i></div></div><div class="card s6"><div class="title">Gastos do mês</div><div class="metric">${money(mm.expense+mm.vale+mm.pro)}</div><div class="muted">Limite ${money(sl)}</div><div class="progress"><i style="width:${sl?Math.min(100,(mm.expense+mm.vale+mm.pro)/sl*100):0}%"></i></div></div><div class="card s3"><div class="muted">Vendas hoje</div><div class="metric ok">${money(m.sales)}</div></div><div class="card s3"><div class="muted">Despesas</div><div class="metric bad">${money(m.expense)}</div></div><div class="card s3"><div class="muted">Vales</div><div class="metric warn">${money(m.vale)}</div></div><div class="card s3"><div class="muted">Pró-labore</div><div class="metric">${money(m.pro)}</div></div></div>`}
function prevFinal(){let a=db.closures.filter(c=>c.date<today()).sort((x,y)=>x.date.localeCompare(y.date)).pop();return a?.final||{dinheiro:0,pix:0,cartao:0}}
function renderCash(){let d=db.cashDays[today()],opts=db.accounts.map(a=>`<option value="${a.id}">${a.name}</option>`).join("");content.innerHTML=`<div class="card"><div class="title">Caixa do dia</div>${!d?`<div class="hint">O saldo final do último fechamento já aparece como sugestão.</div><div class="row">${db.accounts.map(a=>`<div class="f4"><label>Saldo inicial — ${a.name}</label><input id="o_${a.id}" type="number" value="${Number(prevFinal()[a.id]||0).toFixed(2)}"></div>`).join("")}<div class="f12"><button class="primary" onclick="openCash()">Abrir caixa</button></div></div>`:`<div class="toolbar"><button onclick="quick('venda','dinheiro')">+ Dinheiro</button><button onclick="quick('venda','pix')">+ PIX</button><button onclick="quick('venda','cartao')">+ Cartão</button><button onclick="quick('vale','dinheiro')">+ Vale</button><button onclick="quick('prolabore','dinheiro')">+ Pró-labore</button><button onclick="quick('despesa','dinheiro')">+ Despesa</button></div><div class="row" style="margin-top:12px"><div class="f3"><label>Tipo</label><select id="txType"><option value="venda">Venda</option><option value="entrada">Outra entrada</option><option value="despesa">Despesa</option><option value="vale">Vale</option><option value="prolabore">Pró-labore</option></select></div><div class="f3"><label>Conta</label><select id="txAccount">${opts}</select></div><div class="f3"><label>Categoria</label><input id="txCat"></div><div class="f3"><label>Valor</label><input id="txValue" type="number"></div><div class="f12"><label>Observação</label><input id="txNote"></div><div class="f12"><button class="primary" onclick="addTx()">Salvar lançamento</button></div></div>`}</div><div class="card" style="margin-top:12px"><div class="title">Lançamentos de hoje</div>${txTable(dayTx())}</div>`}
function openCash(){let open={};db.accounts.forEach(a=>open[a.id]=Number(document.getElementById("o_"+a.id).value||0));db.cashDays[today()]={open,openedAt:new Date().toISOString()};save();render()}
function quick(t,a){txType.value=t;txAccount.value=a;txCat.value=t==="venda"?"Venda":t==="vale"?"Vale":t==="prolabore"?"Pró-labore":"Despesa";txValue.focus()}
function addTx(){let v=Number(txValue.value||0);if(v<=0)return alert("Informe o valor.");db.tx.push({id:uid(),date:today(),time:new Date().toLocaleTimeString("pt-BR"),type:txType.value,account:txAccount.value,category:txCat.value.trim(),amount:v,note:txNote.value.trim()});save();render()}
function txTable(a){return a.length?`<div class="scroll"><table><thead><tr><th>Hora</th><th>Tipo</th><th>Conta</th><th>Categoria</th><th>Valor</th></tr></thead><tbody>${a.slice().reverse().map(x=>`<tr><td>${x.time}</td><td>${x.type}</td><td>${x.account}</td><td>${x.category||"-"}</td><td>${money(x.amount)}</td></tr>`).join("")}</tbody></table></div>`:`<div class="muted">Nenhum lançamento.</div>`}
function renderClose(){let d=db.cashDays[today()],m=metrics(),c=db.closures.find(x=>x.date===today());content.innerHTML=`<div class="card"><div class="title">Fechamento de caixa</div>${!d?`<div class="hint">Abra o caixa primeiro.</div>`:c?`<div class="hint">Fechado. Saldo final será levado ao próximo dia.</div>${db.accounts.map(a=>`<p><b>${a.name}:</b> ${money(c.final[a.id])} — diferença ${money(c.diff[a.id])}</p>`).join("")}`:`<div class="row">${db.accounts.map(a=>`<div class="f4"><label>${a.name} esperado ${money(m.bal[a.id])}</label><input id="f_${a.id}" type="number" value="${m.bal[a.id].toFixed(2)}"></div>`).join("")}<div class="f12"><button class="primary" onclick="closeCash()">Fechar caixa</button></div></div>`}</div>`}
function closeCash(){let m=metrics(),final={},diff={};db.accounts.forEach(a=>{final[a.id]=Number(document.getElementById("f_"+a.id).value||0);diff[a.id]=final[a.id]-m.bal[a.id]});db.closures.push({id:uid(),date:today(),final,diff});save();render()}