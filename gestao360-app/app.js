const tabs=[["painel","Painel"],["caixa","Caixa"],["fechamento","Fechamento"],["compras","Compras"],["estoque","Estoque"],["relatorios","Relatórios"],["rh","RH"],["fiscal","Fiscal"],["metas","Metas"]];let current="painel";function renderNav(){nav.innerHTML=tabs.map(([id,n])=>`<button class="${id===current?"active":""}" onclick="go('${id}')">${n}</button>`).join("")}function go(id){current=id;renderNav();render()}function render(){if(current==="painel")renderDashboard();else if(current==="caixa")renderCash();else if(current==="fechamento")renderClose();else if(current==="compras")renderPurchases();else if(current==="estoque")renderProducts();else if(current==="relatorios")renderReports();else if(current==="rh")renderHR();else if(current==="fiscal")renderFiscal();else renderSettings()}checkSession();